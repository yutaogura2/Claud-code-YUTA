# 簡易バックテスト Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 価格ベースの逆張り/モメンタムを簡易ポートフォリオで過去検証し、指標と資産曲線を CLI/Web に出す。

**Architecture:** `data.fetch_history` で長期履歴を別キャッシュ取得。`screener/backtest.py` が価格専用スコアで一定間隔リバランスを試算し指標を算出。`screen.fetch_histories` で並列取得し、CLI `backtest`・Web `/backtest` から呼ぶ。

**Tech Stack:** Python, pandas, 既存 indicators, Flask, pytest。追加依存なし。

## Global Constraints

- 追加依存なし。Python実行は venv 経由: `.venv\Scripts\python`（PowerShell、プロジェクトルートから）。
- 価格ベースのみ（逆張りの低PER/低PBRは除外）。取引コスト・配当・スリッページ無視。サバイバーシップ・バイアスあり → 出力に注記。
- 長期履歴のキャッシュキーは `<ticker>_hist_<period>`（既存1年取得と独立）。
- equity 列は先頭 1.0（開始時資本）を含む規約。
- テストはネット非依存（合成 histories / cache事前書き込み / score スタブ）。

---

### Task 1: data.fetch_history（長期履歴・別キャッシュ）

**Files:**
- Modify: `screener/data.py`
- Modify: `tests/test_data.py`

**Interfaces:**
- Consumes: 既存 `_read_cache`/`_write_cache`、`io`、`pd`、`yf`、`time`。
- Produces: `data.fetch_history(ticker, period="3y", ttl=86400, retries=3) -> pandas.DataFrame | None`。

- [ ] **Step 1: 失敗するテストを書く**

`tests/test_data.py` に追記:
```python
def test_fetch_history_uses_cache(tmp_path, monkeypatch):
    import pandas as pd
    monkeypatch.setattr(dataio, "CACHE_DIR", tmp_path)
    idx = pd.date_range("2024-01-01", periods=3, freq="D")
    df = pd.DataFrame({"Close": [1.0, 2.0, 3.0], "Volume": [1, 1, 1]}, index=idx)
    dataio._write_cache("9999.T_hist_3y", {"history": df.to_json(orient="split", date_format="iso")})
    out = dataio.fetch_history("9999.T", period="3y")
    assert out is not None and list(out["Close"]) == [1.0, 2.0, 3.0]
```

- [ ] **Step 2: 失敗を確認**

Run: `.venv\Scripts\python -m pytest tests/test_data.py -k "fetch_history" -v`
Expected: FAIL（`AttributeError: ... 'fetch_history'`）

- [ ] **Step 3: 実装**

`screener/data.py` の `fetch_financials` 関数の前（または `fetch` の後）に追記:
```python
def fetch_history(ticker: str, period: str = "3y", ttl: int = 86400, retries: int = 3):
    """長期の日次OHLCVを取得。キャッシュキーは <ticker>_hist_<period>。失敗は None。"""
    key = f"{ticker}_hist_{period}"
    cached = _read_cache(key, ttl)
    if cached is not None and cached.get("history"):
        h = pd.read_json(io.StringIO(cached["history"]), orient="split")
        if not h.empty:
            h.index = pd.to_datetime(h.index)
        return h

    last_err: Exception | None = None
    for attempt in range(retries):
        try:
            hist = yf.Ticker(ticker).history(period=period, auto_adjust=True)
            if hist.empty:
                raise ValueError("empty history")
            _write_cache(key, {"history": hist.to_json(orient="split", date_format="iso")})
            return hist
        except Exception as e:  # noqa: BLE001
            last_err = e
            time.sleep(1.5 * (attempt + 1))
    print(f"  [warn] {ticker} 履歴取得失敗: {last_err}")
    return None
```

- [ ] **Step 4: テスト成功を確認**

Run: `.venv\Scripts\python -m pytest tests/test_data.py -k "fetch_history" -v`
Expected: PASS

- [ ] **Step 5: コミット**

```bash
git add screener/data.py tests/test_data.py
git commit -m "feat(backtest): 長期履歴取得 fetch_history を追加"
```

---

### Task 2: backtest._metrics（指標計算）

**Files:**
- Create: `screener/backtest.py`
- Create: `tests/test_backtest.py`

**Interfaces:**
- Produces: `backtest._metrics(equity: list[float], years: float) -> dict`
  （キー: `累積%`, `CAGR%`, `最大DD%`, `期間`, `勝率%`）。`equity[0]` は 1.0 規約。

- [ ] **Step 1: 失敗するテストを書く**

Create `tests/test_backtest.py`:
```python
from screener import backtest


def test_metrics_basic():
    m = backtest._metrics([1.0, 1.1, 0.99, 1.2], years=1.0)
    assert m["累積%"] == 20.0
    assert m["最大DD%"] == -10.0       # 1.1→0.99 で -10%
    assert m["期間"] == 3
    assert m["勝率%"] == 66.7          # 3期中2期プラス
    assert m["CAGR%"] == 20.0
```

- [ ] **Step 2: 失敗を確認**

Run: `.venv\Scripts\python -m pytest tests/test_backtest.py -v`
Expected: FAIL（`ModuleNotFoundError: No module named 'screener.backtest'`）

- [ ] **Step 3: 実装**

Create `screener/backtest.py`:
```python
"""価格ベースの簡易ポートフォリオ・バックテスト。

逆張り/モメンタムの価格専用スコアで一定間隔リバランスを試算する。
取引コスト・配当・スリッページは無視。サバイバーシップ・バイアスあり。
"""
from __future__ import annotations

from . import indicators as ind


def _metrics(equity, years):
    """equity（先頭1.0規約）から指標を算出。"""
    n = len(equity)
    total = (equity[-1] - 1) if n else 0.0
    rets = [equity[i] / equity[i - 1] - 1 for i in range(1, n) if equity[i - 1]]
    win = (sum(1 for r in rets if r > 0) / len(rets) * 100) if rets else 0.0
    peak, mdd = (equity[0] if n else 1.0), 0.0
    for v in equity:
        peak = max(peak, v)
        mdd = min(mdd, v / peak - 1)
    cagr = (equity[-1] ** (1 / years) - 1) if (n and equity[-1] > 0 and years > 0) else 0.0
    return {"累積%": round(total * 100, 1), "CAGR%": round(cagr * 100, 1),
            "最大DD%": round(mdd * 100, 1), "期間": len(rets), "勝率%": round(win, 1)}
```

- [ ] **Step 4: テスト成功を確認**

Run: `.venv\Scripts\python -m pytest tests/test_backtest.py -v`
Expected: PASS

- [ ] **Step 5: コミット**

```bash
git add screener/backtest.py tests/test_backtest.py
git commit -m "feat(backtest): 指標計算 _metrics を追加"
```

---

### Task 3: 価格専用スコア（_contrarian_score・_momentum_score）

**Files:**
- Modify: `screener/backtest.py`
- Modify: `tests/test_backtest.py`

**Interfaces:**
- Consumes: `indicators`（rsi/sma/deviation_pct/bollinger/volume_ratio/macd/roc）。
- Produces: `backtest._contrarian_score(close, vol) -> int`、`backtest._momentum_score(close, vol) -> int`。

- [ ] **Step 1: 失敗するテストを書く**

`tests/test_backtest.py` に追記:
```python
import numpy as np
import pandas as pd


def _series(values):
    idx = pd.date_range("2023-01-01", periods=len(values), freq="B")
    return pd.Series(values, index=idx)


def test_contrarian_score_on_decline():
    close = _series(np.linspace(200, 100, 260))   # 下落基調
    vol = _series([1000] * 260)
    s = backtest._contrarian_score(close, vol)
    assert 0 <= s <= 4 and s >= 1                  # 売られすぎ条件に該当


def test_momentum_score_on_rise():
    close = _series(np.linspace(100, 200, 260))    # 上昇基調
    vol = _series([1000] * 260)
    s = backtest._momentum_score(close, vol)
    assert 0 <= s <= 5 and s >= 1                  # 強さ条件に該当
```

- [ ] **Step 2: 失敗を確認**

Run: `.venv\Scripts\python -m pytest tests/test_backtest.py -k "score" -v`
Expected: FAIL（`AttributeError: ... '_contrarian_score'`）

- [ ] **Step 3: 実装**

`screener/backtest.py` に追記:
```python
def _contrarian_score(close, vol):
    """価格専用の逆張りスコア（0-4）。"""
    if len(close) < 20:
        return 0
    price = float(close.iloc[-1])
    rsi = ind.rsi(close)
    dev200 = ind.deviation_pct(price, ind.sma(close, 200))
    _, _, bb_low = ind.bollinger(close)
    vr = ind.volume_ratio(vol)
    return int(sum([
        rsi <= 30,
        dev200 < 0,
        price < bb_low if bb_low == bb_low else False,
        vr >= 1.5 if vr == vr else False,
    ]))


def _momentum_score(close, vol):
    """価格専用のモメンタムスコア（0-5）。"""
    if len(close) < 30:
        return 0
    price = float(close.iloc[-1])
    rsi = ind.rsi(close)
    macd_line, macd_sig = ind.macd(close)
    roc_now = ind.roc(close, 12)
    roc_prev = ind.roc(close.iloc[:-5], 12)
    vr = ind.volume_ratio(vol)
    dev200 = ind.deviation_pct(price, ind.sma(close, 200))
    return int(sum([
        rsi >= 70,
        macd_line > macd_sig,
        (roc_now == roc_now and roc_prev == roc_prev and roc_now > roc_prev),
        vr >= 1.2 if vr == vr else False,
        dev200 > 0,
    ]))
```

- [ ] **Step 4: テスト成功を確認**

Run: `.venv\Scripts\python -m pytest tests/test_backtest.py -k "score" -v`
Expected: PASS

- [ ] **Step 5: コミット**

```bash
git add screener/backtest.py tests/test_backtest.py
git commit -m "feat(backtest): 価格専用スコアを追加"
```

---

### Task 4: run_strategy・run_backtest（試算本体）

**Files:**
- Modify: `screener/backtest.py`
- Modify: `tests/test_backtest.py`

**Interfaces:**
- Consumes: `_metrics`, `_contrarian_score`, `_momentum_score`。
- Produces: `backtest.run_strategy(histories, score_fn, top_n, step, warmup) -> {"dates", "equity", "metrics"}`、
  `backtest.run_backtest(histories, benchmark_close, cfg) -> {"contrarian", "momentum", "benchmark"}`。
  `histories`: `{ticker: DataFrame(Close,Volume,...) }`。`benchmark_close`: `pandas.Series | None`。

- [ ] **Step 1: 失敗するテストを書く**

`tests/test_backtest.py` に追記:
```python
def test_run_strategy_picks_riser():
    idx = pd.date_range("2023-01-01", periods=10, freq="B")
    up = pd.DataFrame({"Close": np.linspace(10, 20, 10), "Volume": [1] * 10}, index=idx)
    down = pd.DataFrame({"Close": np.linspace(20, 10, 10), "Volume": [1] * 10}, index=idx)
    histories = {"UP.T": up, "DOWN.T": down}
    score = lambda close, vol: 1 if close.iloc[-1] > close.iloc[0] else 0  # 上昇を選好
    res = backtest.run_strategy(histories, score, top_n=1, step=1, warmup=2)
    assert res["equity"][0] == 1.0
    assert res["metrics"]["累積%"] > 0          # 上昇銘柄を保有→プラス
    assert len(res["dates"]) >= 1


def test_run_backtest_keys():
    idx = pd.date_range("2023-01-01", periods=300, freq="B")
    a = pd.DataFrame({"Close": np.linspace(100, 200, 300), "Volume": [1000] * 300}, index=idx)
    b = pd.DataFrame({"Close": np.linspace(200, 100, 300), "Volume": [1000] * 300}, index=idx)
    bench = pd.Series(np.linspace(100, 150, 300), index=idx)
    cfg = {"backtest": {"top_n": 1, "rebalance_days": 21, "warmup_days": 60}}
    res = backtest.run_backtest({"A.T": a, "B.T": b}, bench, cfg)
    assert set(res) == {"contrarian", "momentum", "benchmark"}
    assert res["benchmark"] is not None and "累積%" in res["benchmark"]["metrics"]
```

- [ ] **Step 2: 失敗を確認**

Run: `.venv\Scripts\python -m pytest tests/test_backtest.py -k "run_" -v`
Expected: FAIL（`AttributeError: ... 'run_strategy'`）

- [ ] **Step 3: 実装**

`screener/backtest.py` に追記:
```python
def run_strategy(histories, score_fn, top_n, step, warmup):
    """一定間隔リバランスの等金額ポートフォリオを試算。"""
    if not histories:
        return {"dates": [], "equity": [1.0], "metrics": _metrics([1.0], 1.0)}
    ref = max((h.index for h in histories.values()), key=len)
    if len(ref) <= warmup:
        return {"dates": [], "equity": [1.0], "metrics": _metrics([1.0], 1.0)}

    points = list(range(warmup, len(ref) - 1, step))
    dates, equity = [], [1.0]
    for k in range(len(points) - 1):
        d, d2 = ref[points[k]], ref[points[k + 1]]
        scored = []
        for t, h in histories.items():
            sub = h.loc[:d]
            if len(sub) < warmup:
                continue
            sc = score_fn(sub["Close"], sub["Volume"])
            if sc > 0:
                scored.append((sc, t))
        scored.sort(key=lambda x: x[0], reverse=True)
        picks = [t for _, t in scored[:top_n]]
        rets = []
        for t in picks:
            h = histories[t]
            try:
                p1 = float(h.loc[:d]["Close"].iloc[-1])
                p2 = float(h.loc[:d2]["Close"].iloc[-1])
                if p1:
                    rets.append(p2 / p1 - 1)
            except Exception:  # noqa: BLE001
                pass
        r = sum(rets) / len(rets) if rets else 0.0
        equity.append(equity[-1] * (1 + r))
        dates.append(d2)
    years = max(1e-9, max(1, len(points) - 1) * step / 252)
    return {"dates": dates, "equity": equity, "metrics": _metrics(equity, years)}


def run_backtest(histories, benchmark_close, cfg):
    bt = cfg.get("backtest", {})
    top_n = bt.get("top_n", 5)
    step = bt.get("rebalance_days", 21)
    warmup = bt.get("warmup_days", 200)
    out = {
        "contrarian": run_strategy(histories, _contrarian_score, top_n, step, warmup),
        "momentum": run_strategy(histories, _momentum_score, top_n, step, warmup),
        "benchmark": None,
    }
    if benchmark_close is not None and len(benchmark_close) > warmup:
        c = benchmark_close.iloc[warmup:]
        base = float(c.iloc[0])
        eq = [1.0] + [float(v) / base for v in c] if base else [1.0]
        years = max(1e-9, len(c) / 252)
        out["benchmark"] = {"equity": eq, "metrics": _metrics(eq, years)}
    return out
```

- [ ] **Step 4: テスト成功を確認**

Run: `.venv\Scripts\python -m pytest tests/test_backtest.py -v`
Expected: PASS（全件）

- [ ] **Step 5: コミット**

```bash
git add screener/backtest.py tests/test_backtest.py
git commit -m "feat(backtest): リバランス試算 run_strategy/run_backtest を追加"
```

---

### Task 5: 配線（config・screen.fetch_histories・CLI・Web・README）

**Files:**
- Modify: `config.yaml`
- Modify: `screener/screen.py`
- Modify: `main.py`
- Modify: `web/app.py`
- Modify: `README.md`
- Modify: `tests/test_web.py`

**Interfaces:**
- Consumes: `backtest.run_backtest`, `data.fetch_history`, `screen.fetch_histories`。
- Produces: `screen.fetch_histories(cfg) -> dict[str, DataFrame]`、CLI `backtest` モード、Web `/backtest`。

- [ ] **Step 1: config.yaml に backtest を追記**

`config.yaml` の `fetch:` ブロックの直後に追記:
```yaml
# 簡易バックテスト（価格ベース）
backtest:
  period: "3y"        # 取得期間
  top_n: 5            # 各期の保有銘柄数
  rebalance_days: 21  # リバランス間隔(営業日≒1ヶ月)
  warmup_days: 200    # 指標安定までの助走(SMA200等)
```

- [ ] **Step 2: screen.fetch_histories を追加**

`screener/screen.py` の `fetch_universe` の後に追記:
```python
def fetch_histories(cfg):
    """universe の長期履歴を並列取得し {ticker: DataFrame} を返す。"""
    tickers = cfg["universe"]
    ttl = cfg.get("cache_ttl", 86400)
    period = (cfg.get("backtest") or {}).get("period", "3y")
    workers = max(1, (cfg.get("fetch") or {}).get("max_workers", 8))
    print(f"履歴取得中… {len(tickers)}銘柄（並列{workers}, {period}）")
    with ThreadPoolExecutor(max_workers=workers) as ex:
        hists = list(ex.map(lambda t: dataio.fetch_history(t, period, ttl), tickers))
    out = {t: h for t, h in zip(tickers, hists) if h is not None and not h.empty}
    print(f"完了 {len(out)}件")
    return out
```

- [ ] **Step 3: main.py に backtest モードを追加**

`main.py` の import 群に追記:
```python
from screener import backtest
```

`main.py` の `run_market` 関数の後に追記:
```python
def run_backtest_cmd(cfg):
    hists = screen.fetch_histories(cfg)
    period = (cfg.get("backtest") or {}).get("period", "3y")
    bench = dataio.fetch_history(cfg["index"], period, cfg.get("cache_ttl", 86400))
    bench_close = bench["Close"] if (bench is not None and not bench.empty) else None
    res = backtest.run_backtest(hists, bench_close, cfg)
    print("\n■ 簡易バックテスト（価格ベース・コスト無視・サバイバーシップバイアスあり）")
    rows = [
        {"戦略": "逆張り", **res["contrarian"]["metrics"]},
        {"戦略": "モメンタム", **res["momentum"]["metrics"]},
    ]
    if res["benchmark"]:
        rows.append({"戦略": "日経平均(買い持ち)", **res["benchmark"]["metrics"]})
    _print_table(rows, len(rows))
    print("※傾向把握用。実運用成績を保証するものではありません。")
```

`main.py` の import に `from screener import data as dataio` が無ければ追記（現状mainには無いため追加）:
```python
from screener import data as dataio
```

`main.py` の argparse choices に `backtest` を追加:
```python
    p.add_argument("mode", choices=["value", "contrarian", "momentum",
                                    "market", "all", "report", "alpha", "backtest"])
```

`main.py` の `run_market` 分岐の直後（`stocks = screen.fetch_universe(cfg)` の前）に追記:
```python
    if a.mode == "backtest":
        run_backtest_cmd(cfg)
        return
```

- [ ] **Step 4: 実データ確認（CLI）**

Run: `.venv\Scripts\python main.py backtest`
Expected: 「履歴取得中… 40銘柄（並列8, 3y）」の後に逆張り/モメンタム/日経平均の指標表が表示される。

- [ ] **Step 5: Web /backtest を追加**

`web/app.py` の import 群に追記:
```python
from screener import backtest  # noqa: E402
```

`web/app.py` の `report_md` 関数の後（`if __name__` の前）に追記:
```python
@app.route("/backtest")
def backtest_page():
    hists = screen.fetch_histories(CFG)
    period = (CFG.get("backtest") or {}).get("period", "3y")
    bench = dataio.fetch_history(CFG["index"], period, CFG.get("cache_ttl", 86400))
    bench_close = bench["Close"] if (bench is not None and not bench.empty) else None
    res = backtest.run_backtest(hists, bench_close, CFG)

    rows = [{"戦略": "逆張り", **res["contrarian"]["metrics"]},
            {"戦略": "モメンタム", **res["momentum"]["metrics"]}]
    if res["benchmark"]:
        rows.append({"戦略": "日経平均(買い持ち)", **res["benchmark"]["metrics"]})
    table = report._table_html(rows, len(rows), 100)
    charts = (f"<h3>逆張り 資産曲線</h3>{report._svg_line(res['contrarian']['equity'])}"
              f"<h3>モメンタム 資産曲線</h3>{report._svg_line(res['momentum']['equity'])}")
    note = ("<p class='empty'>価格ベース・取引コスト無視・サバイバーシップバイアスあり。"
            "傾向把握用で実運用成績を保証しません。</p>")
    body = f"{_nav()}<h1>簡易バックテスト</h1>{table}{charts}{note}"
    return _page(body)
```

`web/app.py` の `home()` の `<p><a href="/report">…</a></p>` の直後に追記:
```python
<p><a href="/backtest">簡易バックテスト（価格ベース）</a></p>
```

- [ ] **Step 6: Web テストを追記**

`tests/test_web.py` に追記:
```python
def test_backtest_page(client, monkeypatch):
    import numpy as np
    import pandas as pd
    from screener import data as dataio
    idx = pd.date_range("2023-01-01", periods=300, freq="B")
    a = pd.DataFrame({"Close": np.linspace(100, 200, 300), "Volume": [1000] * 300}, index=idx)
    monkeypatch.setattr(screen, "fetch_histories", lambda cfg: {"7203.T": a})
    monkeypatch.setattr(dataio, "fetch_history",
                        lambda t, period="3y", ttl=86400: pd.DataFrame(
                            {"Close": np.linspace(100, 150, 300), "Volume": [1] * 300}, index=idx))
    html = client.get("/backtest").get_data(as_text=True)
    assert "簡易バックテスト" in html and "逆張り" in html and "モメンタム" in html
```

- [ ] **Step 7: 全テスト・スモーク**

Run: `.venv\Scripts\python -m pytest tests/ -q`
Expected: PASS（全件）

Run: `.venv\Scripts\python -c "from web.app import app; print('/backtest' in [str(r) for r in app.url_map.iter_rules()])"`
Expected: `True`

- [ ] **Step 8: README 追記・コミット**

`README.md` の使い方コマンド一覧に追記（`web` の前）:
```
.\run.ps1 backtest      # 価格ベースの簡易バックテスト（逆張り/モメンタム vs 日経平均）
```
「## 設定」に `backtest.*`、Web段落に「『簡易バックテスト』ページあり」を一文追記。

```bash
git add config.yaml screener/screen.py main.py web/app.py tests/test_web.py README.md
git commit -m "feat(backtest): CLI/Web配線と並列履歴取得を追加"
```

---

## Self-Review

- **Spec coverage:** fetch_history(別キャッシュ)=Task1 / _metrics=Task2 / 価格専用スコア=Task3 / run_strategy・run_backtest(リバランス・ベンチ)=Task4 / config・fetch_histories・CLI backtest・Web /backtest・README=Task5 / 割り切り注記=CLI/Web出力。全項目カバー。スコープ外(value/alpha BT・コストモデル)は未着手で正しい。
- **Placeholder scan:** プレースホルダなし。全ステップ実コード/実コマンド。
- **Type consistency:** `fetch_history(ticker,period,ttl,retries)->DataFrame|None`(T1) を Task5 fetch_histories/CLI/Web が消費。`_metrics(equity,years)->dict`(T2) を T4 が使用。`_contrarian_score/_momentum_score(close,vol)->int`(T3) を T4 run_strategy が score_fn として使用。`run_strategy(histories,score_fn,top_n,step,warmup)->{dates,equity,metrics}`・`run_backtest(histories,benchmark_close,cfg)->{contrarian,momentum,benchmark}`(T4) を Task5 が消費。equity 先頭1.0規約を _metrics/run_strategy/benchmark で統一。
- **注意:** main に `from screener import data as dataio` を追加（現状mainはdataio未import）。web は既に dataio import 済。`_print_table` は main 既存を流用（rows のキーが見出し）。
